// port number at which torcd server starts
port = 3699;

// process configuration
processes = (
        {
            name = "echo";
            threads = 50;
            cmd = "/usr/bin/echo";
        },
        {
            name = "cat";
            threads = 50;
            cmd = "/usr/bin/cat";
        }
        );
